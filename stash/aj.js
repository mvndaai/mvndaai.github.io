
    /*
    // AJ Style
    function Player() {
    }
    Player.create = function(p, boardName, shipVisibility){
        p.boardName = boardName;
        p.shipVisibility = shipVisibility;
        p.ships = [];
        p.attacks = [];
    };

    Player.addShip = function(p, headLocation,orientation,length){
        var ship = {"head":headLocation,"orientation":orientation,"length":length,"sunk":false};
        p.ships.push(ship);
    };

    var user = {};

    Player.create(user,"user",true);
    Player.addShip(user,"2.4","horizontal",2);
    console.log(user);
    */
